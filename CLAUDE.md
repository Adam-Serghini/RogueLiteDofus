# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

**V0 + V1 are implemented** (TS + Vite, vanilla DOM). The original V0/V1 spec files have been removed from the repo — the implementation itself is now the reference for what exists. The only remaining doc is:

- `GDD-roguelite-dofus.md` — the full game vision. Background only; consult it for *why* a system exists, never to widen scope beyond the current tranche.

Implemented beyond the original specs (added by playtest iteration): a **4-element system** (`Element = terre | feu | eau | air`, `types.ts`) driven by 4 offensive stats (force→terre, intelligence→feu, agilité→air, **chance→eau**; the earlier wakfu/stasis elements were removed), **8 classes** (Iop/Cra/Eniripsa/Sadida/Sram/Feca/Ecaflip/**Ouginak** — **Sadida is disabled**: kept in `CLASSES`/data for tests & saves but filtered out of `classesDisponibles()` in `run.ts` pending a rework). The **Ouginak** (user-designed kit, `ouginak.test.ts`): Proie (1 PA, UNIQUE permanent mark — the whole team lifesteals 10% of damage dealt to it, hook in `infligerDegats` via the `proie` effect), **Rage** (`Combatant.rage`, +5%/charge dmg cap 3 — `RAGE_MAX`/`RAGE_BONUS` in combat.ts, gained AFTER each rage-granting cast), Molosse (3 PA + self −10% DR), Dépouille (4 PA, +50% per OTHER enemy on the target's row — `bonusParEnnemiLigneCible`), Tétanisation (4 PA, `tetanise` effect: target can't aim at the back row for its next turn — enforced in `ciblesValides`, its answer to Tir courbe shooters; the original push-to-back-row Tibias rider was cut as game-breaking with Dépouille), Tibias (4 PA `zoneLigne` AoE + −10% dégâts infligés 1t), Apaisement (3 PA, requires ≥1 Rage, consumes ALL, heals 8-12/charge — `consommeRage`). Spell icons pending: sort buttons now show the spell NAME when the icon 404s (`.sort-nom-fallback`), a **recruitment system** (start with 2, recruit/heal at tavernes, team of 4 max), and **equipment** (`ITEMS`/`PANOPLIES` in `data.ts`; **4 slots** — `arme`/`coiffe`/`cape`/`anneau` (amulette/ceinture/bottes were removed to cut micro-management and leave room for future item rarity); each panoplie has 4 pieces with set bonuses at 2/4; **12 zone-bound panoplies** (one per zone, mapped in `BUTIN_ZONE`) — Aventurier, Paysan, Bouftou, Tofu, Gladiateur (Akadémie des Gobs), Kankreblath, Fantomatique (Maison Fantôme), Scarafeuille (defensive/all-element resist), Forgeron (force + prospection), Larve (vita+resists), Corailleur (chance/eau), **Kwak** (all-stat, best T1 set, Nid du Kwakwa); item stats are **rolled at drop** from `[min,max]` ranges (`Item.rolls` → an `ItemInstance` with concrete `stats` via `rollItem`), so two copies differ; duplicates allowed; **per-run** loot — `RunState.inventaire` + `PersoState.equipement` hold `ItemInstance` objects, lost on death; **Prospection** boosts drop rate). Equipment stats/PV/resistances fold into the combatant via `bonusEquipement`/`pvMaxPerso` in `run.ts`.

Also: **Archimonstres & Dofus Ocre** — any enemy can rarely spawn as a boosted "Archi" variant (`ARCHI` in `data.ts`); defeating it captures the species once (persistent `Meta.archis`), tracked in a per-zone **Bestiaire** (`showBestiaire` — lists **every** species of each zone incl. boss/miniboss, with capture state on the archi-capable ones; 32 capturable species across the 12 zones; the Kankreblath and Larves-donjon species genuinely have no archi on Metamob — archis attach to the base/outdoor variants). Zone rosters follow the **real DofusDB dungeon compositions** (`api.dofusdb.fr/dungeons/{id}` → `monsters[]` + `bosses[]`) — check there before inventing species. The **Dofus Ocre** is no longer a boss drop — it unlocks in tiers every 50 captures (`OCRE_PALIERS`), granting escalating team passives (PA + damage) via `bonusEquipe(meta)` (combines Dofus + Ocre). Not reachable with the current ~16 monsters by design (framework for a bigger bestiary, ~286).

**Kamas & HDV are IN** (per-run currency in `RunState.kamas`, lost on death; earned on victories via `gainKamas` — node type × toile, ±15% variance — and by selling at the **Hôtel de vente** map node, `hdv` NodeType, tile `cases/hdv.png`, excluded from Incarnam; stock = `genererStockHDV`, PREMIUM shop: current-toile items only at épique/légendaire, next-toile items from rare up (~40% of slots when it exists; commun/rare local gear comes from fights); buy price `KAMAS.prix` by rarity × toile, resale 50%; constants in `KAMAS` in data.ts; kamas shown on the map sidebar and in the recap). Still out of scope (next tranches): Commerce/Forgemagie nodes, multi-world forks, pet/multi-ring slots, item level requirements. (The weapon slot now exists; pet/multi-ring remain deferred.)

Both docs are in French; the game's domain vocabulary (PA, sorts, esquive, élément de frappe, Dofus…) is French and is kept as-is in code identifiers and data.

## Commands

- `npm run dev` — Vite dev server (hot reload) at http://localhost:5173
- `npm test` — run the Vitest suite once. `npm run test:watch` to watch.
- A single test: `npx vitest run src/combat.test.ts -t "règle de ligne"` (`-t` matches the `describe`/`it` name).
- `npm run typecheck` — `tsc --noEmit`, strict mode.
- `npm run build` — typecheck + production bundle to `dist/`.
- `npm run sim` — **headless balancing harness** (`src/sim.ts` via `vitest.sim.config.ts`; kept out of the `*.test.ts` glob so `npm test` ignores it). Replays every encounter N times (IA both camps, seeded RNG) for a reference multi-element team at each zone's expected level, in three gear states (**NU / MI 2-pièces / SET complet**), and prints a win% · turns · HP-left table (flags: `falaise 2→4p`, `stalemate?`…). Tune constants in `data.ts`/`progression.ts`, re-run, compare. **Caveat**: the sim's `controllerIA` heals optimally and never mis-plays → it's a difficulty *floor*; real play is harder (esp. boss fights). See the `sim-harness` memory.
- `node scripts/fetch-assets.mjs` — one-shot fetch of monster sprites from DofusDB (name.fr → gfxId → PNG into `public/assets/monstres/`; skips existing files; direct gfxId overrides for species without an exact DofusDB homonym).

## What the game is

A turn-based tactical roguelite in the Dofus universe, played in the browser. The single loop V0 must validate: **tactical combat + a permanent relic (Dofus) that survives death and makes the next run easier**. Everything is judged against one question — *is the combat fun?*

## Tech stack

- **TypeScript + Vite**, vanilla — **no React, no Canvas**. UI is plain DOM. The point is to test *feel*, not visuals.
- **Data-driven**: classes, spells, monsters, encounters, map-generation params and tuning constants live in `src/data.ts`, not hardcoded in logic.
- **Vitest** for the pure modules (`combat`, `progression`, `carte`) — validated headlessly, independent of the DOM.
- Persistence: the `Meta` object (owned Dofus, captured `archis`, `runs`/`victoires` counters) survives runs, saved to `localStorage` (key `rld_meta_v0`, with rétro-compat defaults in `chargerMeta`). Player *settings* (end-turn key, auto-end-turn, default formation row, per-class preferred element) persist separately under `rld_settings_v0` (`src/config.ts`). The **run in progress** persists under `rld_run_v0` (`sauverRunEnCours`/`chargerRunEnCours`/`effacerRunEnCours` in `run.ts`): saved after every resolved board node and at zone transitions; a combat in progress is NOT saved (refresh = redo the node); cleared on wipe/victory/abandon (abandon counts as a failed run); the start screen offers Reprendre/Abandonner when a save exists. Levels/points/HP live in `RunState` and reset on death.
- Deploy: pushing `main` publishes to GitHub Pages via `.github/workflows/deploy.yml` (`GITHUB_ACTIONS=true` sets Vite `base` to `/RogueLiteDofus/`).

## Code map & architecture

The hard line in this codebase: **`combat.ts` is pure** (no DOM, no `localStorage`) and **`ui.ts` is presentation only** (no game rules). Everything is wired in `main.ts`. Keep new logic on the correct side of that line — it's why the engine is unit-testable.

- `src/types.ts` — the data model (interfaces straight from the spec): combat types, `Progression`, equipment types (`Item` with `rolls`, the rolled `ItemInstance`, `EquipSlot`), and map types (`MapNode`, `GameMap`).
- `src/data.ts` — all content & tuning: `SORTS` (**7 classes**: Iop, Cra, Eniripsa, Sadida, **Sram**, **Feca**, **Ecaflip** + monsters), `CLASSES`, `MONSTRES` (bestiary across 12 zones), `DOFUS` (31-relic catalogue, only Pourpre has an effect), `COMBATS` (per-zone encounters; boss encounters = **boss + miniboss**), **`ZONES`** (12 zones, flat) + **`TRANCHES`** (level-bracket groupings; t1 = the 12 zone ids in play order — Incarnam → Champs d'Astrub → Tainéla → Tofus → Akadémie des Gobs → Kankreblath → Maison Fantôme → Scarafeuilles → Forgerons → Larves → Grotte Hesque → Nid du Kwakwa, ordered by official dungeon level; t2-t5 empty/inactive; event dungeons Nowel/Halouine/Pwak are reserved and must never enter tranche zone lists; each zone has `pools = {normales, elite, boss}`), `XP_PAR_TYPE`, `TAVERNE_PCT`, `GEN_CARTE` (the board is a **Pokelike diamond lattice**: alternating staggered rows of 2 — columns ±0.5 — and 3 — columns −1/0/+1 — with edges = exact diagonals |Δcol| = 0.5; row count 10-12 rounded to even so the last 2-row converges on the single donjon). Each class/monster has an optional `img` (path under `public/assets/…`). Sprites/icons and base stats/resistances are pulled from **DofusDB** (`api.dofusdb.fr` JSON + `/img/monsters/{gfxId}.png` CDN — keyed by **gfxId**, not monster id; the website itself is a JS SPA and not fetchable). **Archimonstre names** (`archiNom`) come from **Metamob**, which is more complete than DofusDB for archis — see the `archimonstre-source-metamob` memory for how to read its bundled static catalog without an API key.
- `src/combat.ts` — **pure engine**. `runCombat()` is the async turn loop, decoupled from input via **controllers** (`Controller = (acteur, combatants) => Action | null`). The UI passes a promise-based controller for the player; `controllerIA` drives enemies. RNG injected (`hooks.rng`) for deterministic tests. Reads final `c.stats` (via `statsEffectives`, which adds temp stat buffs; it never knows about levels except `c.niveau`, baked for `+x/lvl` scaling). Handles the **support + new-class mechanics** (see below). Dedicated handlers `lancerTarot`/`lancerEspritFelin` for the random Ecaflip spells.
- `src/progression.ts` — **pure**. Levels/XP/points: `xpRequis`, `coutPoint` (escalating 1→2→3 by points already invested), `gagnerXP`, `investir`/`investirN` (bulk allocation, `n = Infinity` for the UI's « Max » button), `restat`, `statsFinales`, `pvMaxFor`, `multOffensif`, `multSoin`, `pctRembPA` (PA-refund chance from Chance, used by Flèche magique). No DOM/storage. `STAT_KEYS` = investable stats (Soin & Prospection are class-only, not investable).
- `src/config.ts` — player settings (`Settings`): end-turn key, `autoFinTour`, default `formation` (classe → grid cell 0-7) and per-class preferred `elements`, persisted to `localStorage` (`rld_settings_v0`) with validation + defaults on load. `nouvelleRun` reads it to seed positions/elements.
- `src/carte.ts` — **pure**. `genererCarte(rng, pools)` builds one zone's node graph by rows with connectivity guarantees (combat ids drawn from the zone's `pools`); `atteignables(carte)` / `noeud(carte,id)` for navigation.
- `src/run.ts` — `RunState`/`PersoState` (per-character progression + current HP + `elementChoisi`, persisted across nodes). **Dynamic roster**: `nouvelleRun(choix)` builds the team from chosen class ids (run starts with **2 of 7**); `recruter`/`propositionsRecrutement`/`classesHorsEquipe`/`equipePleine` drive Taverne recruitment (team max 4, recruit-at-team-level, swap when full). `EQUIPE_DEPART` is just the default used by tests. Combatant factories **bake final stats** into a `Combatant` (`combattantDepuisPerso`, `equipeCombattante`, `synchroniserPV` writes back HP + chosen element), `soignerEquipe`, `Meta` persistence (`bonusDegatsDofus`).
- `src/ui.ts` — DOM rendering + player controller (click spell → click target) + screens: combat, **`showChoixEquipe`** (pick 2 of 7 at run start), **`showTaverne`** (heal OR recruit one of 2 random candidates; replace a member when full), start/transition/wipe/dofus, `showStatPanel` (level-up/Otomai), `showCarte` (SVG node graph), `showFormation`. Combatant cards show element ronds (clickable to switch strike element), crit/dégât-crit/soin/dgts-finaux mini-stats, all-4 resistances, prospection (allies only). Sprites/icons via the `asset(categorie, id)` convention (`/assets/<cat>/<id>.png`) with `onerror` fallback — drop a file, it shows; absent, it's removed (nothing breaks). Class portraits exist for all 7 classes under `public/assets/classes/`; individual spell icons may still be absent and fall back gracefully.
- `src/main.ts` — orchestration: accueil → for each zone of the **active `TRANCHES` entry** (une run = une tranche; t1 = 12 zones, t2-t5 locked placeholders shown grayed on the start screen), `jouerZone` generates that zone's board and navigates it until the donjon (boss+miniboss) is beaten → next zone; wipe anywhere ends the run. HP/levels persist across zones; only `Meta.dofus` survives death. The long-term content roadmap (5 tranches, rarity, boss signatures) lives in `PLAN-CONTENU.md`.

Two key seams keep things testable and decoupled:
1. **Combatants are rebuilt per fight** from `RunState` — final stats and `pvMax` are computed in `run.ts` and baked into the `Combatant`, so `combat.ts` stays ignorant of levels/points. HP persists via `PersoState.pvActuels` (written back by `synchroniserPV`), not by keeping the combatant object around.
2. **The async controller pattern**: `runCombat` `await`s the active camp's controller, so the same loop runs headless (IA vs IA in tests) or interactively (promise resolved by a DOM click) with no branching in the engine.

### Core combat rules that are easy to get wrong

- **Élément de frappe** (`elementDeFrappe`, `combat.ts`): a character has no fixed element. Element ↔ stat is `terre→force`, `feu→intelligence`, `air→agilité`, `eau→chance`. The strike element is the **player's `elementChoisi` if it is one of their 2 strongest elements, otherwise their single strongest** — so the clickable element ronds let a player pick between their top-two, not any element. All of a character's damage uses that element, and target resistances are applied per that element.
- **Grid & line rule (SYMMETRIC, 4×2 per camp)**: `position` is a grid cell **0-7** — cells **0-3 = ligne avant**, 4-7 = arrière (`estAvant(c) = position < 4`, `combat.ts`). `ennemi_ligne` spells hit only the target camp's front-row living; if the front row is empty, the back becomes exposed (`ligneFront`). Applies to **both** camps. `ennemi_tous` ignores it; rebound (Flèche perçante) hops by ascending position; taunt (`provoque`) overrides. Placement is free: all-front, all-back, mono-tank, etc. Players placed via **formation** (`config.formation` = `{classeId: cell}` → `PersoState.position`), edited from the board grid (`showFormation`); enemies placed by encounter `position` cells in `data.ts` (up to 8). Combat cards are vertical, grouped into front/back rows per camp.
- **Damage pipeline** (order matters, see `degatsCible` in `combat.ts`): esquive check (Agilité, capped) → roll (or max if `maxRoll` charge) → add élément-de-frappe stat × scaling → crit (Force, capped, *adds* damage via Agilité, does not double) → `degatsInfliges` debuff → rebound mult → resistance (unless `ignoreResistances`) → Dofus team bonus (`ctx.playerDamageBonus`, player camp only) → `multOffensif` (Intelligence, capped, all casters) → round, floor at 0.
- **Support mechanics** (Eniripsa kit, all in `combat.ts`): damage goes through `infligerDegats` → **shield (`bouclier`) absorbs before HP**; `poison`/`hot` are timed effects in `effets[]` that **tick at the start of the actor's turn** in `effetsDebutTour` (poison can kill, then transmits to the combatant `derriere` if `transmet`); heals via `soigner` (incl. `soinComplet`, `allie_tous`, `soinEquipeRatio` lifesteal-to-team); `appliquerSoutien` applies bouclier/HoT/`dissipe`/`paGain`/`bonusProchainSortPct`; per-target **cooldowns** live on `acteur.cooldowns` and are enforced inside `ciblesValides`. `allie_tous`/`soi`/`invocation` spells resolve with no target click.
- **Invocation & init (Sadida kit)**: the turn loop re-picks the next actor each step by **effective initiative** (`initOf` = base + `initiative` effects), so a mid-round init debuff (Déferlante/Étreinte) actually delays enemies that haven't acted yet. **Invocations** (`estInvocation`, e.g. the Poupée de garde via `invoquerPoupee`) occupy a slot, are skipped in the loop, and can `provoque` — enforced in `ciblesValides` (enemies must target a provoking ally first). One poupée per summoner. `Vigueur des bois` sets `bonusOffensifProchain`, a one-shot % consumed by the next damage cast.
- **Secondary-stat caps are mandatory**: crit, esquive, puissance offensive are capped (~50%) in the formulas. Don't remove the `Math.min` caps.
- **HP persists between fights/nodes** within a run — the Taverne node is the only heal. A wipe ends the run but **must not erase `Meta.dofus`**.
- **PV model**: `pvMax = pvBase + vitalitéFinale × PV_PAR_VITA` (spec-literal). Monster damage in `data.ts` is tuned against the resulting (inflated) player HP — retune both together.

## Scope discipline

Each tranche is deliberately narrow; the spec's validation criteria define "done". Current build: **7 playable classes** (Iop/Cra/Eniripsa/Sram/Feca/Ecaflip/Ouginak; Sadida disabled pending rework), **4 elements**, in-run levels/points, recruitment (start at 2, taverne recruit/heal, max 4), **equipment** (12 zone-bound panoplies, 4 slots, rolled-stat per-run loot), **Tranche 1 = 12 sequential zones** ordered by official dungeon level (see `TRANCHES` above; **EVERY t1 boss drops a Dofus, one relic per zone GROUP**: zones 1-6 → Dofawa (+1 vita/copie, cap 10), zones 7-12 → Argenté (+1% resist all/copie, cap 10) — Pourpre & Turquoise deliberately held back for later tranches, distribution test in zones.test.ts), a 31-relic collection (Pourpre/Dofawa/Argenté have effects in `DOFUS_EFFETS`; Ocre via archi captures), and a start screen showing the 5 tranches (t2-t5 locked). **All 12 bosses have a signature mechanic** on top of the shared `ecrasement/charge/morsure` kit. Damage-spell signatures: a 6-PA spell with a rider, listed FIRST in `sorts` (aggressive IA plays highest-cost first, ties broken by list order) and gated by `cooldownTours`. Engine-level signatures: Kwakwa's `mueElementaire` (each of its turns: 65% resist everywhere except one random element at 0 — `appliquerMueElementaire`), Grunob's `bonusParAllieLigne` passive (+15% damage per living same-row ally, folded into the damage mult in `lancerSort`), and invocation-type signatures played by `iaAgressif` in priority when `invocationUtile` (Kankreblath `invoqueMonstre` summons a zone monster — summons are FULL combatants that play their turns, unlike the Sadida Poupée; Shin Larve's 2-PA ponte lets her summon AND attack; Boostache `ressuscite` revives a fallen non-boss ally at 50% HP). Tests in `boss.test.ts`. Sim note: summoned actors inflate the sim's per-actor turn counts on those boss rows. **Item rarity is LIVE for ALL 12 toiles (T1 COMPLETE — the legacy panoplie drop path is now dead code kept only as fallback for unknown zones)**: items authored in `scripts/items.csv` (the user designs in a spreadsheet; 4 rows per item = commun/rare/epique/legendaire with FIXED stats per tier — no rolls, no set bonuses by design). **ALL toiles use the ADAPTIVE stat model** (toiles 1-2 were redone: ONE pano per zone — Aventurier for Incarnam, Champ Champ for Astrub, Bouftou for Tainéla — with the original toile stat budgets; the old per-element sets/extras were culled, stale save items are purged on load in chargerRunEnCours). The adaptive stat (`adapt` CSV column → `TierItem.adaptatif`): ONE item line adds to the wearer's `statPrincipale` (chosen element's stat, else auto-alloc, else highest invested elemental) — one pano per zone instead of one per element; toiles 1-2 keep per-element stats until the user redoes them. New CSV columns: `adapt`, `soin`, `source` (boss = donjon-only drop, elite = combat_dur-only — tenterButin's FIRST draw comes from the node's exclusive pool; the HDV sells the FULL toile catalogue incl. boss/elite exclusives — premium tiers still apply), `att_cible` (tous = weapon hits back row, e.g. Arc en Corne de Bouftou), `att_vamp` (weapon lifesteal ratio, e.g. Ergot Mina 0.5). Boufcoiffe/Boufcape Royale = the first boss-named rares; toile 4 (Tofus) adds SPECIAL mechanics: `special` CSV column → `Item.paGamble` (Chance d'Ecaflip ring: each turn 33% +1 PA / 66% −1 PA — negative EV by design, applied via `appliquerChanceEcaflip` at turn start, carried on `Combatant.paGamble`) and `Item.ligneAvant` (Cape Edepee: equippable ONLY on front-row characters — enforced in `equiper`/`peutEquiper`, greyed in the inventory, and the Formation screen blocks moving a wearer to the back row). Toiles 5 (Akadémie/Grunob) & 6 (Kankreblath) add: a `source` value **`elite_boss`** (Dora, Bâton Tont'Ata — the item sits in BOTH exclusive pools; Armurerie dedupes it into one card with an Élite/Boss badge) and three more specials carried like paGamble (`Item`+`Combatant` fields, first-wearer-wins in `combattantDepuisPerso`): `riposte_avant` → `riposteAvant: 0.33` (Sabre Shodanwa — plugs into the Duel `contre` riposte in `infligerDegats`, active only when `estAvant`), `esquive_arriere` → `esquiveArriere: 0.10` (Baguette Rikiki — added to the dodge roll when NOT front row), `soin_recus` → `soinDegatsRecus: 0.02` (Goyave — heals that fraction of each hit taken; deliberately tiny, flagged for retune). All 16 toile-5/6 names are REAL DofusDB items (exact icon matches, incl. Dora which IS a Chapeau). Weapon attack profiles are Claude-designed (the user's sheet doesn't carry them). Toiles 7 (Maison Fantôme), 8 (Scarafeuilles), 9 (Forgerons) add 5 more specials: `change_ligne` → `Item.changeLigne` (Dagues Eurfolles — grants the SORTS spell `changer_ligne`, 1 PA, swap front↔back row via `caseLibreRangeeOpposee`, same column preferred; appended to the wearer's spell list in `combattantDepuisPerso`; note: with auto-fin-tour the turn no longer auto-ends while 1 PA remains since moving is always possible), `perce_res` → `Item.perceResistances: 0.5` (Dagues Aj'Deh'La — weapon attack counts only half the target's resistances, `BaseDegats.perceResistances` in `degatsAvec`), `frappe_derriere` → `Item.frappeDerriere` (Masse Aj Taye — also hits the back-row enemy closest to the target's column, `derriereEnLigne`), `prosp_pv_manquant` → `Item.prospParPvManquant: 0.2` (Caskoffre — prospection bonus per missing HP of the wearer, computed INSIDE `prospectionEquipe` so it's evaluated at loot time), `mult_kamas` → `Item.multKamas: 1.2` (Ann'or — `multKamasEquipe(run)` applied at the two combat-kamas credit sites in main.ts, NOT on HDV sales). **Toiles 7 & 9 are deliberately utilitarian** (crit/prospection, near-zero vita) so gear barely raises survivability — their zones were retuned for near-NU play: Boostache pv 599→330 agi 29→16 + ressuscite 0.5→0.35 & cd 3→4 (the rez loop starved common-gear DPS; sim also under-reads this fight because controllerIA focus-fires lowest-HP rezzes instead of the boss — real players win by focusing Boostache), Scarabosse pv 746→600 force 41→30 resists 0.2→0.15 + carapace 1.0→0.4, Coffre pv 786→430 force 46→23 + mâchoire 26-34@0.6→18-24@0.48, escorts trimmed (sim: fan 46/scr 98/frg 56 SET). Toiles 10 (Larves — tank/gemmes: Chapeau Lithique, La Mokette…), 11 (Grotte Hesque — the REAL Akwadala family + Masse du Corailleur; first use of the `soin` stat), 12 (Kwakwa — one piece PER ELEMENT among Kwakoiffe/Kwape/Kwakanneau/Kwaklame, boss items = the real Kwakwa-tier names) add 6 more specials: `bouclier_debut` → `Item.bouclierDebut: 0.15` (Bonnet Spairance — starting shield, set in combattantDepuisPerso AFTER the etatCombatInitial spread), `poison_arme` (Scalpel de Bworknroll — armeSort gets the existing `poison` field), `soin_allie` → `Spell.soinAllieBlesseRatio` (Masse du Corailleur — heals the most-wounded ally for 20% of damage), `retrait_pa` (Arc des Rivages — reuses the Fracas `retraitPA` spell field, 30% chance), `element_libre` → `Combatant.elementLibre` (Kwakwaffe — the engine already accepts any elementChoisi, the top-2 limit was UI-only: the selector shows all 4 ronds; THE counter to the Kwakwa mue), `renaissance` → `Combatant.renaissance`/`renaissancesRestantes` (Kwakwanneau — revives once per combat at 30% PV via `verifierRenaissance`, hooked in infligerDegats AND the poison-death path). All 24 toile-10/12 names are exact DofusDB items (zero overrides). Retunes: Shin Larve pv 1059→760 chance 46→34 + Larves Dorée/Émeraude trimmed (sim MI 21/SET 91), Kwakwa pv 1023→780 offense ~×0.87 + **mue 0.55→0.5** (sim SET 52 — heavily under-read: the sim never element-switches against the mue and can't wear the boss-loot Kwakwaffe; real play is much faster). **When a toile replaces a legacy pano zone, re-tune that zone's monsters** (toile commun budget is leaner than legacy panoplies — Tainéla got PV ×0.7 + boss offense ×0.72; Astrub PV ×0.85 + Tournesol offense ×0.85; Tofus PV ×0.7 + pack offense ×0.75 + Batofu burst halved — pique 15-21@0.45; Akadémie: Grunob pv 482→410 force 31→18 + bonusParAllieLigne 0.10→0.06 + Gobaladée trimmed — sim MI 18/SET 95; Kankreblath left as-is at MI 23/SET 79. Legacy panoplies of toile'd zones stay defined in data.ts (harmless, never dropped) — only toiles 1-3 legacy sets were culled.) Item icons: `node scripts/audit-icons.mjs` (exact-name match + SURCHARGES map for invented names) → `node scripts/import-items.mjs` generates `src/items_gen.ts` (`ITEMS_TOILES` merged into `ITEMS`, `BUTIN_TOILES` pools by toile = zone index+1 in t1 play order). Drop: `tirerRarete` (weights 60/25/12/3 in `RARETE_INFO`), `rollItem` freezes tier stats into the `ItemInstance` (`rarete`/`resistances`/`pa` fields), `tenterButin` now takes a ZONE id (toile pool → 4 uniform draws; legacy zones fall back to panoplie pieces). New equipment channels: `pa` (paBonus on the combatant), `crit` (flat % added to Force-derived crit in combat+UI), per-tier weapon `attaque`. UI: rarity halos/name colors (`rarete-*`/`inom-*`), stat chips (`itemStatsHtml`). Zones 2-12 still legacy panoplies until the user's CSV rows are added (paste into items.csv, re-run import). The sim still measures legacy sets. Everything else in the "out of scope" list above is **deferred** — resist pulling it in. **Boss rooms are 4v3/4v4 and bosses have 10 PA** (signature 6 PA + attack 4 PA the same turn; boss offensive stats were scaled ~×0.6 to compensate the doubled action rate; rooms add 1-2 zone species incl. back-row shooters — designed after playtest feedback that 2-enemy boss rooms were too simple). **Deliberately hard per the user's request** — after the L50 pass (XP_PAR_TYPE 110/195 × `1 + XP_PAR_TOILE(0.3) × (toile−1)`; **POINTS_PAR_NIVEAU = 3** and **levels are CAPPED at the active tranche's max** — `gagnerXP(p, gain, niveauMax)` discards surplus XP, cap fed by `niveauMaxTranche()` in run.ts; sim zone-entry levels ≈ 1/7/12/15/19/22/26/29/33/36/39/42, real play hits the 50 cap near the end. At 3 pts/level T1 ends ≈147 main-stat (all cost-1); the 200/300 cost thresholds bite in T2+ giving ~248/315/365 at L100/150/200 — the long-term stat curve. Zone-2+ monster PV was rescaled twice and now sits ≈×0.9-1.3 of pre-L50 values) and two sim-honesty fixes (bosses measured at END-of-zone level since the donjon is the last node; toile zones measured wearing the best-per-stat COMMUN rarity items instead of legacy panoplies), sim SET win% ramps ~100→87→82→68 on toile'd zones then 93-100 on legacy-pano zones (they'll be re-tuned as their toiles arrive). **The sim reference build now invests 25% vitalité / 75% main stat and weights vita ×4 in gear picks** — the old full-glass-cannon floor made 10-PA bosses read 0% against lean toile gear (Batofu one-shot ~65-HP heroes) when real players simply wear Le Houde and take some vita (Kwakwa finale 56% at the non-adapting floor; rare/épique/légendaire drops and element-switching make real play easier). Tournesol was retuned for its slot as boss #2 (drain 60%, 4v3 room, pv/int trimmed, Gardienne healer softened); Kardorim for boss #1 (4v2 room, pv 145 — playtest: players reach him with ~2 commun items, sim MI 85%/SET 100%). Geared normals stay trivial. **Elite nodes (combat_dur) get a pack-wide modifier drawn at MAP GENERATION** (`MapNode.eliteModif`, set in carte.ts `enrichir`, shown on the map node label + hover tooltip — playtest showed surprise modifiers were run-killers; catalogue `MODIFICATEURS_ELITE` in data.ts, values toned to +20%/+20%PV/+1PA, applied by `appliquerModificateurElite(_, _, modifId)` with random fallback for zaap-revealed fights and old saves; reward = donjon-rate loot; the sim applies modifiers on its elite rows). **End-of-run recap** (`showRecap`): per-hero damage via the engine's `onDegats` hook, MVP, counters — `RunStats` on `RunState`, serialized with the run save. **Achievements** (`SUCCES` catalogue + `verifierSucces` in run.ts, persisted in `Meta.succes`, checked at run end, shown in the recap and via the 🏆 button on the start screen; rewards deferred to the item system). **Armurerie** (item collection, mirror of the Bestiaire): `Meta.collection` maps itemId → best rarity ever obtained (`"base"` for legacy no-rarity items), recorded by `enregistrerCollection` (run.ts) at drop time (`recompenserButin` in main.ts) and on HDV purchase (`showHDV` now takes `meta`); `showArmurerie` lists every zone's catalogue (toile pools normales + élite/boss badges, else legacy pano pieces) — never-obtained items ghosted, obtained ones solid with the halo/name-color of the best rarity; buttons on the start screen and map top bar (`/assets/menu/armurerie.png`, 🛡️ text fallback while the asset doesn't exist). Values in `data.ts`/`progression.ts` are meant to be adjusted. Crit is a multiplicative bonus (affects players AND monsters).
